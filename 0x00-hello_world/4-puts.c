#include <stdio.h>
#include <string.h>

/* puts print the characters saved in the string variable */

int main(void)
{
	char string[70] = "\"Programming is like building a multilingual puzzle";

	puts(string);
	return (0);
}
