#include "holberton.h"
/**
 * _strlen - function that returns the length of a string..
 * @s: string
 * Return: the value of the variable count.
 */
int void print_rev(char *s);nt count = 0;

	while (s[count] != '\0')
	{
		count++;
	}
}
